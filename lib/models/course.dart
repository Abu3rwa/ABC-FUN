import 'package:flutter/material.dart';

class Course {
  final id;
  final title;
  final name;
  final description;
  final lessons;

  const Course(
      {this.id, this.title, this.name, this.description, this.lessons});
}
